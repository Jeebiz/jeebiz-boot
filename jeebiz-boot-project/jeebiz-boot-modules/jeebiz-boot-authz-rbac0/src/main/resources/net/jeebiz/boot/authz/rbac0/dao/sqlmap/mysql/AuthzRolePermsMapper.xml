<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.jeebiz.boot.authz.rbac0.dao.IAuthzRolePermsDao">

    <insert id="setPerms">
    	insert into SYS_AUTHZ_ROLE_PERMS(R_ID,PERMS)
    	values <foreach collection="perms" item="item" separator =",">
		(#{roleId},#{item})
        </foreach>
    </insert>
    
	<delete id="delPerms">
		DELETE FROM SYS_AUTHZ_ROLE_PERMS WHERE R_ID = #{roleId} AND t.R_ID != 1
		<if test="perms != null and perms.size() > 0">
		<foreach collection="perms" item="item" separator =" OR " open=" AND ( " close=")">
		PERMS = #{item}
        </foreach>
		</if>
    </delete>
    
	<select id="getPermissions" resultType="java.lang.String" parameterType="java.lang.String">
		SELECT t.PERMS AS perms 
		FROM SYS_AUTHZ_ROLE_PERMS t
		WHERE t.R_ID = #{roleId}
	</select>
    
</mapper>